[project]
name = "selenium-example"
version = "0.1.0"
description = "Steel + Selenium: drive a cloud browser through the WebDriver protocol"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0.0",
    "selenium>=4.18.1",
    "steel-sdk>=0.9.0",
]
