[project]
name = "playwright-py"
version = "0.1.0"
description = "Steel + Playwright (Python): drive a cloud browser over CDP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.48.0",
    "python-dotenv>=1.0.0",
    "steel-sdk>=0.9.0",
]
