[project]
name = "scrape-py"
version = "0.1.0"
description = "Steel Scrape API (Python): markdown, screenshot, and PDF to disk"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0.0",
    "steel-sdk>=0.9.0",
]
