# Runtime
NODE_ENV=development

# OpenAI
# Your OpenAI API key (required)
OPENAI_API_KEY=
# Optional org ID
OPENAI_ORG_ID=
# A cost-effective model; web search will be enabled via the API call/tooling layer
OPENAI_MODEL=gpt-5-nano

# Steel.dev
# Your Steel.dev API key (required)
STEEL_API_KEY=
# Scrape endpoint (do not change unless Steel.dev updates)
STEEL_SCRAPE_ENDPOINT=https://api.steel.dev/v1/scrape

# Brave Search
# Your Brave Search API key (required)
BRAVE_API_KEY=
# API endpoint (default shown)
BRAVE_SEARCH_ENDPOINT=https://api.search.brave.com/res/v1/web/search
# Search country code (e.g., US)
BRAVE_SEARCH_COUNTRY=US
# Search language (e.g., en)
BRAVE_SEARCH_LANG=en
# Safe search setting: off | moderate | strict
BRAVE_SAFESEARCH=moderate

# Search behavior
# Number of URLs to return from the web search before scraping
SEARCH_TOP_K=10
# Timeout (ms) for outbound requests (OpenAI + Steel.dev + Brave)
REQUEST_TIMEOUT_MS=5000

QUERY="How do prediction markets provide hedging oppurtunities and potential liquidity against broader market positions?"

# CORS
# Comma-separated origins (e.g., http://localhost:5173,https://your.app)
CORS_ORIGINS=*
