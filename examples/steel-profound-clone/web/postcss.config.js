/**
 * PostCSS configuration for Vite + React + Tailwind CSS
 * Tailwind directives are processed here and autoprefixer adds vendor prefixes.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
