[project]
name = "files-py"
version = "0.1.0"
description = "Steel + Files API (Python): upload a CSV into a session and render it over CDP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.48.0",
    "python-dotenv>=1.0.0",
    "steel-sdk>=0.17.0",
]
