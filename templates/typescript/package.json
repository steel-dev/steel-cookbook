{
  "name": "steel-cookbook-template-ts",
  "version": "1.0.0",
  "private": true,
  "description": "Template for Steel cookbook examples (TypeScript)",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node index.ts",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "steel-sdk": "^0.15.0",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@vitest/ui": "^2.0.0",
    "ts-node": "^10.9.2",
    "vitest": "^2.0.0"
  }
}
