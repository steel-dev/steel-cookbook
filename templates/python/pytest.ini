[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
markers =
    integration: marks tests as integration tests (requires STEEL_API_KEY)
    unit: marks tests as unit tests (no external dependencies)
